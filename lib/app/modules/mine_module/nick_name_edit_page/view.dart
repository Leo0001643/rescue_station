import 'package:flutter/material.dart';
import 'package:get/get.dart';

import 'logic.dart';

class NickNameEditPagePage extends StatelessWidget {
  const NickNameEditPagePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    final logic = Get.find<NickNameEditPageLogic>();
    final state = Get.find<NickNameEditPageLogic>().state;

    return Container();
  }
}
